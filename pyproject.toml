[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "openenv-agent"
version = "1.0.0"
description = "OpenEnv Agent evaluation Space"
requires-python = ">=3.10"
